{
  "name": "fibeger",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @fibeger/web dev",
    "dev:backend": "cd backend && go run ./cmd/server",
    "build:web": "pnpm --filter @fibeger/web build",
    "check:ui": "pnpm --filter @fibeger/ui lint",
    "check:api-client": "pnpm --filter @fibeger/api-client lint",
    "check:packages": "pnpm --filter @fibeger/ui lint && pnpm --filter @fibeger/api-client lint",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist .next node_modules/.cache"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
