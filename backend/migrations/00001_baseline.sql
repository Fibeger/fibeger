-- +goose Up
-- Baseline migration: schema is managed by GORM AutoMigrate in development.
-- This file documents the intended production migration path.
-- Future schema changes should be added as new migration files.

-- +goose Down
-- No rollback for baseline
