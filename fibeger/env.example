# Database
# For production (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/fibeger?schema=public"

# For local development (SQLite) - uncomment this and comment out the PostgreSQL URL above
# DATABASE_URL="file:./prisma/dev.db"

# NextAuth
# IMPORTANT: Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# This must be set in Vercel environment variables for production!
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# For local development:
NEXTAUTH_URL="http://localhost:3000"

# For production (Vercel):
# Set NEXTAUTH_URL to your actual Vercel URL in Vercel Dashboard
# Example: NEXTAUTH_URL="https://your-app.vercel.app"
